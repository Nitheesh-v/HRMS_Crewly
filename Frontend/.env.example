# Local development: point at your local Backend (Phase 27.12 lives here).
# Production builds may override this with the deployed API URL.
VITE_API_URL=http://localhost:5000/api
VITE_MAX_RESUME_SIZE_MB=5