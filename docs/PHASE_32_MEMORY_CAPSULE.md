# PHASE 32 MEMORY CAPSULE — CREWLY PRODUCTION INFRASTRUCTURE (FINAL)

> Purpose: authoritative Phase-32 state for future Arena sessions. **Repository truth wins over this file** — totals/counts below were verified at close-out (HEAD: 32.18 commit on `arena/01a0b338-hrms-crewly`, parent `cadb09e` auth-UI redesign, before it `6a1b0a6` 32.17, `0700c6f` 32.16, `70a5bac` 32.15, `c9bf996` 32.14, `444f273` 32.13). No secret values anywhere in this file.

## Repo & session truth
- Branch `arena/01a0b338-hrms-crewly`. Backend = Node ESM (`node --test`), Express 5.2, Mongoose 9.9, BullMQ 6.3, ioredis 5.11, jsonwebtoken 9, bcryptjs 3, helmet 8, winston 3, multer 2, pdfkit 0.19, cloudinary 2, razorpay 2. **No legacy `bull`.** Frontend = React + Vite 8 + Tailwind v4 + Redux + react-router.
- Sandbox laws that keep recurring: repo re-clones STALE on session restore → repair = `git fetch origin <branch>` + `git update-ref refs/remotes/origin/<branch> FETCH_HEAD` + `git reset -q --soft FETCH_HEAD && git reset -q` (NEVER `--hard`); node_modules vanish → `npm install` both sides; sandbox `.env` vanishes → recreate gitignored dev `.env` (synthetic values: MONGO_URI/JWT_SECRET/CLIENT_URL/PORT/NODE_ENV/JWT_EXPIRES_IN/FIELD_ENCRYPTION_KEY) or ~14 suites exit on import-time MONGO_URI guards; no mongod/Redis binaries in sandbox; NEVER import `routes/index.js` under node --test (hangs); smoke route modules with `MONGO_URI` only; heredoc apostrophes; grep zero-match kills `&&` chains.

## Phase 32 status (32.1–32.18)
All eighteen units IMPLEMENTED + TESTED (matrix in `docs/PHASE_32_ARCHITECTURE.md` §2). Defect registers: **empty** (32.17 verified zero; close-out re-verified). Test totals at close-out: full ladder **2167/2167** (two consecutive green; one earlier single timing flake, not reproducible); `npm run test:phase32` (NEW, 17 suites) **312/312**; frontend lint **132 pre-existing problems** (113 errors — baseline, zero from Phase 32/auth work); frontend build ✓; dist secret-scan clean (276 files); tracked-file secret scan clean.

## Architecture (authoritative)
Internet → DNS → **CDN/edge (provider NOT SELECTED — 32.16 contract only)** → LB (TLS, 32.3 `config/proxyTrust.js` sole trust point; TRUST_PROXY_MODE direct|loopback|hop|cidr, fail-startup on misconfig, forged X-Forwarded-* inert untrusted — behaviorally pinned) → N stateless API (`node src/server.js`, JWT, no sticky) → Mongo (truth) + Redis + private object storage; BullMQ → N workers (`node src/workers/index.js`). Realtime = 32.11 SSE integrated in API processes (POST ticket → GET stream; one-time 30s Redis tickets; tenant channels `crewly:<env>:realtime:*`; caps 500/process, 5/user; `X-Accel-Buffering: no`). **Chat NOT built. Presence NOT built. Heartbeat ≠ employee status. No surveillance** (geolocation = punch-time one-shot only, verified by grep).

## Invariants (durable laws)
Tenant = `req.companyId` only · auth→tenant→subscription→RBAC→cache (cache NEVER authorizes) · GET never finalizes (offer accept/BGV consent/QR redeem/punch are POST) · attendance append-only (regularization overlays) · PayrollResult immutable/versioned · PaymentStatus never manually PAID · BGV failure ≠ rejection; verifier assignment = authorization · hash-only secrets (JWT reset tokens, kiosk device secrets, kiosk PIN, QR challenges; select:false) · generic errors (no enumeration oracles) · never raw user input into `$regex` (`src/utils/searchInput.js`) · never `rejectUnauthorized:false` · never CORS `*` · no FLUSHALL/FLUSHDB/KEYS/SCAN/wildcard `crewly:*` deletes · queue/cache/realtime namespace `crewly:<env>:` per env (staging can never consume prod jobs) · production local-disk storage refused · files never public-URL · scanners never fake-CLEAN · load/failure tooling CLI-only, production-refusing (exact refusal `REFUSED: NODE_ENV=production …`), no FAIL_*/CHAOS_* env toggles, no chaos HTTP endpoints (all pinned by tests) · error responses generic (stacks only dev) · no new deps without STOP+approval · never import routes/index.js under node --test · `git reset --hard` never · PR only on explicit authorization · `.env` never committed; `.env.example` names+placeholders only (~96 names; frontend uses ONLY `VITE_API_URL` + `VITE_MAX_RESUME_SIZE_MB`).

## Queue / cache / observability / storage truth
- **BullMQ:** at-least-once (never claim exactly-once); `src/workers/registry.js` allowlist Map (28.x infra jobs + phase-added processors; unknown job = loud config fault — pinned); env-prefixed queues; bounded retries; terminal-vs-retry classification vocabulary DEGRADED/RETRYABLE/TERMINAL/UNAVAILABLE/SKIP/CONFIGURATION; duplicate financial job ≠ duplicate money (idempotent commit points, pinned by failureRecovery).
- **Cache:** Redis tenant-scoped keys, generation invalidation, TTL-bounded parsers (`source?.X` strict pattern), degraded = per-request DB (correctness unaffected); `cacheMultiInstance` proves shared invalidation.
- **Rate limits (32.4):** shared Redis tier `crewly:<env>:rl:<sharedName>:<identity>`; identity = trusted effective IP + hashed email dimension (never raw PII); Redis-down → documented per-instance local buckets (32.18 runbook §16).
- **Observability (32.12):** winston → stdout JSON, request/correlation IDs, route-template labels (never raw token URLs), redaction middleware strips query strings + Bearer/JWT/credentialed URIs + token path segments + control chars (behaviorally pinned); coarse process sampler; no APM vendor, no /metrics, no PII telemetry.
- **Storage (32.8):** private durable (Cloudinary-class provider) with signed delivery + authorization gates + `private, no-store`; dev-only inline fallback (production-refused); traversal-refusing keys; bounded sizes/types; temp cleanup.

## CDN / static (32.16)
Provider **NOT SELECTED**. Contract: hashed assets immutable 1y · index.html `no-cache` · public non-versioned short-cache · **every /api/* response `private, no-store, max-age=0`** (32.16 default-deny middleware — pinned incl. `.css`-deception/404/errors) · SSE never cached · assets-before-HTML · retain previous assets · SPA fallback ≠ missing-asset 404 · edge owns compression (API compression intentionally deferred — no double compression) · static-host CSP template documented; HSTS preload deferred. `ChunkLoadErrorBoundary` = user-visible reload, never auto-loop.

## Deployment (32.15)
3 envs, separate Mongo DBs, per-env Redis namespaces · startup tiers fail-fast/DEGRADED/FEATURE-UNAVAILABLE · `npm run config:check` (+`--production` real-JWT law via pure `validateProductionConfig`; dev default `'dev_secret_change_me'` refused <32 chars, name-only errors) · rollout config→worker→API rolling→frontend artifact · rollback = previous artifacts (DB additive, never rolled back casually) · production deploy = manual approval concept only · no Docker/K8s/PM2/CI added · deterministic `start`/`worker` (no watch in prod) · `npm ci` for deploys.

## Project structure (32.18 final)
Backend: controllers 99 FLAT (intentional), routes 55, validators 49, models 124 FLAT (intentional, high import cost), services (attendance/bgv/payroll subdirs + flat), middlewares 29, utils 56, config 10, workers 10, `infrastructure/{observability,realtime,storage}`, `scripts/{load/,preview/(32.18 move),ops CLIs}`, test 196 flat (explicit-path scripts). Frontend: pages ≈25 domain dirs, components, services, routes, layout. **DEFERRED:** controllers/routes/validators domain-nesting → future dedicated structure phase. package.json gained `test:phase32` (17 suites, 312 tests) in 32.18.

## Known limitations (honest)
No CDN/vendor selected (edge WAF/final HSTS/CSP-on-HTML/purge/log-privacy = provider task) · load results are sandbox loopback-mock runner-only — NO "supports N users" claims exist · geolocation spoofable (no biometrics) · at-least-once queues (idempotency handles duplicates) · realtime ephemeral (no offline replay) · no Chat/Presence/AI · degraded limiter/cache modes are per-instance · local-disk fallback dev-only · frontend lint 113 pre-existing errors (cleanup deferred).

## Deferred / future roadmap (PROPOSED, NOT AUTHORIZED — no code exists)
1. Tenant Custom Roles & Advanced RBAC (HR Head/Recruiter/Payroll Admin/… tenant-defined roles) · 2. Crewly Chat Hub · 3. Teams-like Presence & Availability · 4. Crewly AI Assistant · 5. controllers/routes/validators domain nesting phase · 6. provider-specific deployment task (DNS/TLS/origins/cache rules/HSTS/CSP) after vendor selection. Do not assume phase numbers; each needs an explicit build prompt.

## Session workflow reminders
One unit at a time on explicit authorization; commit+push at unit end; handoff ends with the exact awaiting-acceptance line then STOP; checklists stay UNCHECKED for the developer; PowerShell-first guides (`$env:NAME="value"` / `Remove-Item Env:NAME`); report ACTUAL totals; BLOCKED ≠ PASS; never claim malware-scanned; never make private files public; 32.16+ docs live in `docs/PHASE_32_*.md`; the runbooks are `docs/PHASE_32_RUNBOOKS.md`; this capsule + `docs/PHASE_32_ARCHITECTURE.md` are the close-out pair.
